/**
 * css.d.ts: o Vite embute a folha de estilo importada so pelo efeito
 * (`import '@xterm/xterm/css/xterm.css'`). O import nao traz valor nenhum; esta
 * declaracao so diz ao TypeScript que o modulo existe.
 */
declare module '*.css';
